'use strict';

import React, {Component} from 'react';
import PropTypes from 'prop-types';

class GenericRibbonLineHeader extends Component {

  constructor(props) {
    this.state = {
    }
  }
  
  componentDidMount() {
  }

  shouldComponentUpdate(nextProps, nextState) {
    return true;
  }

  componentDidUpdate(prevProps, prevState, snapshot) {
  }

  render() {
    return (
      <div>
      </div>
    )
  }

}

GenericRibbonLineHeader.propTypes = {
}

GenericRibbonLineHeader.defaultProps = {
}

export default GenericRibbonLineHeader;